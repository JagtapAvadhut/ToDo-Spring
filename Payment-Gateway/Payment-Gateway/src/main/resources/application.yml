eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:8761/eureka

spring:
  application:
    name: todo-user
  datasource:
    url: jdbc:mysql://localhost:3306/todo_user
    password: sql123
    username: root
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true